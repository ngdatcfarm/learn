node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Claude Code local settings
.claude/

# Local data dir (không còn dùng sau khi chuyển sang MySQL)
data/

# MySQL dump backups (Step 10a) — generated in-process by dbBackup cron job
backups/
